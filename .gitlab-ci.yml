image: golang:1.12

stages:
  - test
  - build

cache:
  paths:
    - /apt-cache
    - /go/src/

test:
  stage: test
  artifacts:
    paths:
      - test.txt
  script:
    - make test branch='${CI_COMMIT_REF_SLUG}'

test_blog:
  stage: test
  script:
  - cd examples/blog; go run ../../*.go develop

test_iterators:
  stage: test
  script:
    - cd examples/iterators; go run ../../*.go develop

test_simplePage:
  stage: test
  script:
    - cd examples/simplePage; go run ../../*.go develop

benchmark:
  stage: test
  artifacts:
    paths:
      - benchmark.txt
      - benchmark_change.txt
  script:
    - make benchmark

build:
  stage: build
  artifacts:
    paths:
      - dist/
  script:
    - make build
